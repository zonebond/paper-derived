# 任务：按用户指令修改文档树

规则：
1. 只改目标部分；非目标 Section 内容保持逐字不变。
2. 输出必须是完整的 DocumentTree（与 generate 相同结构），保持全部 Section 与层级。
3. 新增 Section 必须标注 status 和 lineage。
4. 风格与文档其余部分一致。

只输出完整 DocumentTree JSON，无任何其他文字。

## 用户指令
